// 配置文件
export default {
	// API基础地址
	baseURL: 'http://localhost:8000',
	
	// 超时时间（增加到30秒）
	timeout: 30000,
	
	// 图片上传大小限制（MB）
	maxImageSize: 10,
	
	// 支持的图片格式
	imageTypes: ['jpg', 'jpeg', 'png', 'gif', 'webp']
}
